# git config --global include.path "~/settings/git/.gitconfig"

[user]
	name = Edward Chen
	email = hi@edch.top
[credential "helperselector"]
	selected = manager-core
[alias]
	st = status
	ci = commit
	br = branch
	co = checkout
	last = log -1 HEAD
